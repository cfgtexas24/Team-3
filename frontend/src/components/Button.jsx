// Import React
import React from 'react';
 
// Button component definition
const MyButtonComponent = ({ onClick, children }) => {
    return (
        <button onClick={onClick} className="my-button">
            fsdfds
        </button>
    );
};

export default MyButtonComponent;
